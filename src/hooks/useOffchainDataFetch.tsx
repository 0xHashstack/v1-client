import React, { useEffect } from "react";

const useOffchainDataFetch = () => {
  useEffect(() => {
    try {
      const fetchOffcahinData = async () => {
        
      };
      fetchOffcahinData();
    } catch (err) {}
  }, []);
  return <div>useOffchainDataFetch</div>;
};

export default useOffchainDataFetch;
