version: 1
frontend:
  phases:
    preBuild:
      commands:
        - rm -rf node_modules
        - nvm i 20.15
        - nvm use 20.15
        - npm i
    build:
      commands:
        - nvm use 20.15
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
